# ─────────────────────────────────────────────────────────────
# SCINT v2 — API Configuration
# ─────────────────────────────────────────────────────────────
# 1. Copy this file and rename the copy to ".env"
# 2. Paste your real keys after the "=" sign (no quotes, no spaces)
# 3. NEVER commit the real .env file to GitHub (it's in .gitignore)
# ─────────────────────────────────────────────────────────────

VIRUSTOTAL_API_KEY=your_virustotal_api_key_here
ABUSEIPDB_API_KEY=your_abuseipdb_api_key_here

# Optional — get free key at https://haveibeenpwned.com/API/Key
HIBP_API_KEY=
